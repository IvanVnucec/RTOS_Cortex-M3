language: generic

services:
  - docker

jobs:
  include:
    - name: build target
      before_install:
        - ./scripts/docker_run.sh
      script:
        - ./scripts/check_build.sh
        # add testing script
        

#    - name: test
#      before_script:
#        - gcc --version
#      script:
#        - cd ${TRAVIS_BUILD_DIR}
#        - make test
#        - bash <(curl -s https://codecov.io/bash)